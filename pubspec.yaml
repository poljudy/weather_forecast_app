name: weather_forecast_app
description: The project is designed to obtain weather via the OpenWeatherMap API. With handy features. :)
version: 0.0.2

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  http: ^0.13.5

dev_dependencies:
  test: ^1.22.1

  lint: ^2.0.1

flutter:
   assets:
    - assets/weather_icons/

screenshots:
  - description: '<weather_forecast_app> package banner'
    path: assets/images/banner.png
  - description: '(RU) This console mini-application shows how to use the package without flutter.'
    path: example/weather_in_console/assets/result_in_console.gif
